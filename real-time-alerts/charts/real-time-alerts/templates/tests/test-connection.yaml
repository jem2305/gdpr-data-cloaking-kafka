apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "real-time-alerts.fullname" . }}-test-connection"
  labels:
    {{- include "real-time-alerts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "real-time-alerts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
